# Telodos.com 作業ルール

このリポジトリは、ユリーカ・ティロドス公式サイトの本番ソースです。ChatGPT / Codexで更新するときは、以下を守ってください。

## 作業フロー

1. `main` へ直接コミットしない。必ず目的の分かる作業ブランチを作る。
2. 変更前に、該当する `src/data/` とコンポーネントを確認する。
3. `npm run validate` が成功した状態でPull Requestを作る。
4. 見た目に関わる変更は、デスクトップとモバイルの両方を確認する。
5. Pull Requestを勝手にマージしない。本番反映はユーザーの確認後に行う。

## デザイン上の固定方針

- 黒と深い紫を基調に、ネオングリーン `#39ff14` を主な差し色として使う。
- 吸血鬼・ゴシックの世界観を維持する。一般的なSaaS風、白基調、過度な角丸カードへ変更しない。
- Heroは「背景・キャラクター発光・メインキャラクター・エフェクト・ロゴ」の多層構成を維持する。
- Hero関連のクラス名は番号ではなく、`hero-character-main` のような役割名を使う。
- 装飾よりもユリーカ本人のビジュアルと公式ロゴを優先する。
- アニメーションは `prefers-reduced-motion` に対応させる。

## コンテンツの更新場所

- ニュース: `src/data/news.ts`
- プロフィール、ナビゲーション、SNS、配信先、ファンクラブ、BOOTH: `src/data/site.ts`
- ガイドライン: `src/data/guidelines.ts`
- デザイン資料: `src/data/design.ts`

掲載内容だけの変更では、原則として `.astro` やCSSを編集しないでください。

## 実装ルール

- ローカル画像は `/images/...` で参照する。`raw.githubusercontent.com` を配信用CDNとして使わない。
- 外部リンクには必要に応じて `target="_blank" rel="noreferrer"` を付ける。
- 画像には意味に合う `alt`、サイズ、遅延読み込みの要否を設定する。
- 新しい依存パッケージは、HTML/CSS/少量のJavaScriptでは解決しにくい場合だけ追加する。
- 共通色・余白・フォントは `src/styles/global.css` の変数を使う。
- JavaScriptは操作に必要な範囲にとどめ、コンテンツ自体はJavaScript無効でも読めるようにする。

## 完了条件

- `npm run check` 成功
- `npm run build` 成功
- 主要リンクに空の `href` がない
- ブラウザコンソールにサイト側のエラーがない
- 320px幅からデスクトップまで横スクロールが発生しない
